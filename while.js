/*Given a digit d (0 to 9), find the sum of the first 50 positive integers (integers > 0) that end with digit d. A number ends with digit d if its last digit is d.
Example 1
Input: d = 1
Output: 12300
Explanation:
The first 50 positive integers ending with 1 are: 1, 11, 21, 31, ..., 491
Their sum is 12300. */
/*
let n = 5;
let factorial = 1;

// Loop to calculate factorial using while
while (n > 0) {
    factorial *= n;
    n--;
}

console.log(`Factorial of 5 is: ${factorial}`);
*/
var dowhile = function(d) {
        // Your code goes here
       
    }
